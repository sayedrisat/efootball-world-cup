import { BarChart3, Camera, Home, Layers3, ShieldCheck, Shuffle, Trophy } from 'lucide-react'
const links = [{ to: '/', label: 'Home', icon: Home }, { to: '/teams', label: 'Teams', icon: Shuffle }, { to: '/groups', label: 'Groups', icon: Layers3 }, { to: '/knockout', label: 'Knockout', icon: Trophy }, { to: '/ranking', label: 'Ranking', icon: BarChart3 }, { to: '/output', label: 'Output', icon: Camera }, { to: '/admin', label: 'Admin', icon: ShieldCheck }]
function Navigation({ currentRoute }) { return <nav className="main-nav"><a className="brand" href="#/"><span>EF</span><strong>E-FOOTBALL CUP</strong></a><div className="nav-links">{links.map(({ to, label, icon: Icon }) => <a className={currentRoute === to ? 'active' : ''} href={`#${to}`} key={to}><Icon size={17} /><span>{label}</span></a>)}</div></nav> }
export default Navigation
